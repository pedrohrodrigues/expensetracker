export interface addIncomeDto {
  title: string;
  category: string;
  amount: number;
  description: string;
  date: Date;
}

export interface getIncomeDto {
  title: string;
  category: string;
  amount: number;
  description: string;
  date: Date;
}
